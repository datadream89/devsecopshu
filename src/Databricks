WITH date_params AS (
  SELECT start_date, end_date
  FROM dates
),
date_range AS (
  SELECT start_date AS date_value
  FROM date_params
  UNION ALL
  SELECT DATEADD(DAY, 1, date_value)
  FROM date_range
  WHERE date_value < (SELECT end_date FROM date_params)
)
SELECT date_value
FROM date_range;
